import { SwapRequestCard } from '@/components/client/SwapRequestCard';
import { Badge } from '@/components/ui/Badge';

// TODO: Wrap with ProtectedRoute — requires authentication
export function SwapRequestPage() {
  return (
    <main>
      {/* Page header */}
      {/* Tabs: Incoming | Outgoing | Completed */}
      {/* SwapRequestCard list for active tab */}
      {/* Empty state per tab */}
    </main>
  );
}
